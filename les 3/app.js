//dit is commentaar voor javascript
// ik denk dat je de tag kan selecteren met let id en let p

let title = document.getElementById("newstitle")
let randomGetal = Math.random();
if (randomGetal == 0) {
    
    title.style.backgroundColor = "#00FF00";
}

else if (randomGetal < 0.2) {

    title.style.backgroundColor = "#000000";
}

else if (randomGetal )